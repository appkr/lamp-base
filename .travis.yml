sudo: required

services:
- docker

before_install:
- ls -d mysql_data/* | grep -v .gitignore | xargs rm -rf
- sleep 10
- docker-compose up -d

script:
- sleep 120
- docker container ls

matrix:
  fast_finish: true
